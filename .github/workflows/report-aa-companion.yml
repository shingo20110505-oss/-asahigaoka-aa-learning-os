name: Report AA Companion Integration
on:
  push:
    paths:
      - '.report-aa-companion-trigger'
permissions:
  actions: read
  contents: write
jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Fetch workflow status
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: |
          python - <<'PY'
          import json, os, urllib.request
          repo=os.environ['REPO']; token=os.environ['GH_TOKEN']
          url=f'https://api.github.com/repos/{repo}/actions/workflows/integrate-aa-companion-v2.yml/runs?per_page=5'
          req=urllib.request.Request(url,headers={'Authorization':f'Bearer {token}','Accept':'application/vnd.github+json','X-GitHub-Api-Version':'2022-11-28'})
          data=json.load(urllib.request.urlopen(req))
          lines=[]
          for r in data.get('workflow_runs',[]):
              lines.append(f"id={r['id']} status={r['status']} conclusion={r.get('conclusion')} head={r['head_sha']} url={r['html_url']}")
          open('AA_COMPANION_INTEGRATION_STATUS.txt','w').write('\n'.join(lines)+'\n')
          PY
      - name: Commit report
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add AA_COMPANION_INTEGRATION_STATUS.txt
          git commit -m "Report AA companion integration status" || exit 0
          git push
