name: Tune English reading quality gate 2026-09-14
on:
  push:
    branches: [main]
    paths: ['.github/workflows/tune-reading-quality-20260914.yml']
  workflow_dispatch:
permissions:
  contents: write
jobs:
  tune:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Repair Groq verifier structural retry
        run: |
          python - <<'PY'
          from pathlib import Path

          p = Path('worker/src/providers/groq.mjs')
          s = p.read_text()
          if "json_object_validation_fallback" not in s:
              start_marker = "  const output = parseGroqJson(payload);\n"
              end_marker = "  return { output, provider: 'groq', model, mode, fallbackFrom };\n}"
              start = s.find(start_marker)
              end = s.find(end_marker, start)
              if start < 0 or end < 0:
                  raise SystemExit('missing Groq provider validation tail')
              end += len(end_marker)
              replacement = """  let output = parseGroqJson(payload);
              let validation = validateGroqOutputAgainstSchema(output, schema);
              if (!validation.ok && schemaName === READING_VERIFIER_SCHEMA_NAME && mode === 'json_schema' && request?.allowJsonObjectFallback !== false) {
                const fallbackBody = buildRequestBody({ model, mode: 'json_object', input, schema, schemaName, systemInstruction, maxOutputTokens, temperature, reasoningEffort });
                const fallback = await sendGroq(apiKey, fallbackBody, timeoutMs);
                if (!fallback.response.ok) throwGroqHttpError(fallback.response, fallback.payload);
                output = parseGroqJson(fallback.payload);
                validation = validateGroqOutputAgainstSchema(output, schema);
                mode = 'json_object_validation_fallback';
                fallbackFrom = 'json_schema_local_validation';
              }
              if (!validation.ok) throw new GroqProviderError(502, 'Groq output did not satisfy the Rise verification schema.', 'groq_schema_mismatch', validation.errors.slice(0, 12).join('|'));
              return { output, provider: 'groq', model, mode, fallbackFrom };
              }"""
              p.write_text(s[:start] + replacement + s[end:])

          test = Path('tests/ai-reading-groq-validation-fallback.mjs')
          if not test.exists():
              test.write_text(r'''import assert from 'node:assert/strict';
          import { callGroqJson } from '../worker/src/providers/groq.mjs';
          import { VERIFIER_SCHEMA } from '../worker/src/index.mjs';

          const sentence = 'Students compared two plans and recorded clear evidence before they changed their final decision.';
          const valid = {
            overallPass: true,
            answers: [0,1,2,3,4].map(questionIndex => ({questionIndex, answerIndex: questionIndex % 4, evidenceQuote: sentence, confidence: 0.9}))
          };
          const malformedButJson = { overallPass: true, answers: [valid.answers[0]] };
          const calls = [];
          const originalFetch = globalThis.fetch;
          globalThis.fetch = async (url, options = {}) => {
            const body = JSON.parse(options.body || '{}');
            calls.push(body);
            const output = calls.length === 1 ? malformedButJson : valid;
            return new Response(JSON.stringify({choices:[{message:{content:JSON.stringify(output)}}]}), {status:200, headers:{'content-type':'application/json'}});
          };
          try {
            const result = await callGroqJson({GROQ_API_KEY:'test-key', GROQ_MODEL:'openai/gpt-oss-20b'}, {
              input:'Blindly solve five questions.',
              schema:VERIFIER_SCHEMA,
              schemaName:'rise_english_blind_verification',
              maxOutputTokens:2500,
              temperature:0,
              reasoningEffort:'low'
            });
            assert.equal(calls.length, 2);
            assert.equal(calls[0].response_format?.type, 'json_schema');
            assert.equal(calls[1].response_format?.type, 'json_object');
            assert.equal(result.mode, 'json_object_validation_fallback');
            assert.equal(result.fallbackFrom, 'json_schema_local_validation');
            assert.deepEqual(result.output, valid);
          } finally {
            globalThis.fetch = originalFetch;
          }
          console.log('Groq reading validation fallback OK: schema-shape drift retries once with JSON object and remains locally validated.');
          ''')
          PY
      - name: Verify Groq verifier repair
        run: |
          set -euo pipefail
          node --check worker/src/providers/groq.mjs
          node tests/ai-provider-contract.mjs
          node tests/ai-reading-groq-contract.mjs
          node tests/ai-reading-groq-validation-fallback.mjs
          node tests/ai-reading-contract.mjs
          node tests/reading-library.mjs
      - name: Commit Groq verifier repair
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add worker/src/providers/groq.mjs tests/ai-reading-groq-validation-fallback.mjs
          if git diff --cached --quiet; then
            echo 'No changes needed'
            exit 0
          fi
          git commit -m 'Retry malformed Groq reading verification in validated JSON mode'
          for n in 1 2 3; do
            git pull --rebase origin main
            if git push origin HEAD:main; then break; fi
            if [ "$n" = 3 ]; then exit 1; fi
          done
