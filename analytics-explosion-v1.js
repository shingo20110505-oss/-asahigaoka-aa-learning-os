(()=>{'use strict';
if(window.__AA_EXPLOSION_ANALYTICS_V1__)return;window.__AA_EXPLOSION_ANALYTICS_V1__=true;
const STREAK_KEY='aa-companion-study-streak-v1',PICK_KEY='aa-companion-voice-daily-pick-v2',LOG_KEY='aa-explosion-draw-history-v1',PENDING_KEY='aa-explosion-predraw-v1';
let lastSig='',timer=0;
function safeJSON(s,f){try{return JSON.parse(s)||f}catch(_){return f}}
function read(k,f){try{return safeJSON(localStorage.getItem(k),f)}catch(_){return f}}
function write(k,v){try{localStorage.setItem(k,JSON.stringify(v))}catch(_){}}
function localDay(d=new Date()){const y=d.getFullYear(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return `${y}-${m}-${day}`}
function parseDay(s){const [y,m,d]=(s||'').split('-').map(Number);return y&&m&&d?new Date(y,m-1,d):null}
function addDays(s,n){const d=parseDay(s);if(!d)return'';d.setDate(d.getDate()+n);return localDay(d)}
function hash01(str){let h=2166136261>>>0;for(let i=0;i<str.length;i++){h^=str.charCodeAt(i);h=Math.imul(h,16777619)}return(h>>>0)/4294967295}
function streakState(){const s=Object.assign({current:0,best:0,totalStudyDays:0,lastCompleted:'',chargePct:0,history:[]},read(STREAK_KEY,{}));s.current=Math.max(0,Number(s.current)||0);s.chargePct=Math.max(0,Math.min(.02,Number(s.chargePct)||0));s.history=Array.isArray(s.history)?s.history:[];return s}
function oddsDetail(today=localDay(),source=null){const s=source||streakState();let count=0;for(let i=1;i<=7;i++)if(s.history.includes(addDays(today,-i)))count++;const basePct=.05,streakPct=s.current*.001,qualityPct=count<=4?0:((count-4)/3)*.005,driftPct=hash01('aa-explosion-'+today)*.003,chargePct=s.chargePct,totalPct=Math.max(.001,basePct+streakPct+qualityPct+driftPct+chargePct);return{date:today,at:Date.now(),totalPct,basePct,streakPct,qualityPct,driftPct,chargePct,streak:s.current,qualityDays:count}}
function fmtPct(v){if(v==null||v==='')return'--';const n=Number(v);return Number.isFinite(n)?`${n.toFixed(4)}%`:'--'}
function signedPct(v){if(v==null||v==='')return'--';const n=Number(v);return Number.isFinite(n)?`${n>=0?'+':''}${n.toFixed(4)}%`:'--'}
function formula(d){if(!d||[d.basePct,d.streakPct,d.chargePct,d.qualityPct,d.driftPct,d.oddsPct??d.totalPct].some(v=>v==null||!Number.isFinite(Number(v))))return'計算式は記録されていません';const total=d.oddsPct??d.totalPct;return `${fmtPct(d.basePct)} + ${fmtPct(d.streakPct)} + ${fmtPct(d.chargePct)} + ${fmtPct(d.qualityPct)} + ${fmtPct(d.driftPct)} = ${fmtPct(total)}`}
function fmtTime(t){if(!t)return'--';const d=new Date(t);return `${String(d.getMonth()+1).padStart(2,'0')}/${String(d.getDate()).padStart(2,'0')} ${String(d.getHours()).padStart(2,'0')}:${String(d.getMinutes()).padStart(2,'0')}`}
function history(){const x=read(LOG_KEY,{version:1,draws:[]});x.draws=Array.isArray(x.draws)?x.draws:[];return x}
function saveDraw(draw,snap){if(!draw?.date)return;const h=history(),old=h.draws.find(x=>x.date===draw.date),rec={date:draw.date,at:Number(draw.at)||Date.now(),explosion:!!draw.explosion,key:draw.key||'',oddsPct:Number.isFinite(Number(snap?.totalPct))?Number(snap.totalPct):null,basePct:Number.isFinite(Number(snap?.basePct))?Number(snap.basePct):null,streakPct:Number.isFinite(Number(snap?.streakPct))?Number(snap.streakPct):null,qualityPct:Number.isFinite(Number(snap?.qualityPct))?Number(snap.qualityPct):null,driftPct:Number.isFinite(Number(snap?.driftPct))?Number(snap.driftPct):null,chargePct:Number.isFinite(Number(snap?.chargePct))?Number(snap.chargePct):null,streak:Number.isFinite(Number(snap?.streak))?Number(snap.streak):(Number(draw.streak)||0),qualityDays:Number.isFinite(Number(snap?.qualityDays))?Number(snap.qualityDays):null,magicLevel:draw.magicLevel||''};if(old)Object.assign(old,rec);else h.draws.push(rec);h.draws.sort((a,b)=>(a.at||0)-(b.at||0));h.draws=h.draws.slice(-365);write(LOG_KEY,h);try{localStorage.removeItem(PENDING_KEY)}catch(_){}schedule(0)}
function snapshotBeforeDraw(date=localDay()){const d=oddsDetail(date);write(PENDING_KEY,d);return d}
function pendingFor(date){const p=read(PENDING_KEY,null);return p?.date===date?p:null}
function recoverExistingPick(){const p=read(PICK_KEY,null);if(!p?.date)return;const h=history();if(h.draws.some(x=>x.date===p.date))return;let snap=pendingFor(p.date);if(!snap&&!p.explosion){const s=streakState(),pre=Object.assign({},s,{chargePct:Math.max(0,(Number(s.chargePct)||0)-.0002)});snap=oddsDetail(p.date,pre)}saveDraw({...p,at:Date.now()},snap)}
function css(){if(document.getElementById('aa-explosion-analytics-css'))return;const s=document.createElement('style');s.id='aa-explosion-analytics-css';s.textContent=`#aaExplosionAnalyticsCard .aaExHero{padding:14px;border:1px solid color-mix(in srgb,var(--purple) 24%,var(--line));border-radius:16px;background:color-mix(in srgb,var(--card) 86%,var(--purple2))}.aaExOdds{font-size:2rem;font-weight:950;line-height:1.1;margin:5px 0}.aaExBreak{font-size:10px;line-height:1.7;color:var(--sub)}.aaExFormula{margin-top:9px;padding:9px 10px;border-radius:10px;background:var(--bg2);border:1px solid var(--line);font-size:11px;font-weight:800;line-height:1.55;word-break:break-word}.aaExFormula small{display:block;font-size:9px;font-weight:700;color:var(--sub);margin-bottom:3px}.aaExToday{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-top:10px}.aaExToday>div{border:1px solid var(--line);border-radius:12px;padding:10px;background:var(--bg2)}.aaExToday b{display:block;font-size:15px}.aaExToday span{font-size:10px;color:var(--sub)}.aaExHit{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;padding:11px 0;border-top:1px solid var(--line);font-size:11px}.aaExHit:first-child{border-top:0}.aaExHit b{font-size:13px}.aaExHitFormula{grid-column:1/-1;font-size:10px;color:var(--sub);background:var(--bg2);border-radius:9px;padding:7px 8px}.aaExBadge{display:inline-block;padding:3px 7px;border-radius:999px;background:var(--purple2);color:var(--purple);font-size:9px;font-weight:900}@media(max-width:520px){.aaExToday{grid-template-columns:1fr}.aaExHit{grid-template-columns:1fr}.aaExHitFormula{grid-column:1}}`;document.head.appendChild(s)}
function analyticsMain(){const h=[...document.querySelectorAll('h2')].find(x=>(x.textContent||'').includes('学習分析'));return h?.closest('main')||null}
function render(){const main=analyticsMain();if(!main){document.getElementById('aaExplosionAnalyticsCard')?.remove();lastSig='';return}css();const today=localDay(),cur=oddsDetail(today),h=history(),todayDraw=h.draws.find(x=>x.date===today),hits=h.draws.filter(x=>x.explosion).slice(-10).reverse();let card=document.getElementById('aaExplosionAnalyticsCard');if(!card){card=document.createElement('section');card.id='aaExplosionAnalyticsCard';card.className='card';const daily=document.getElementById('aaDailyAnalyticsCard');if(daily)daily.insertAdjacentElement('beforebegin',card);else main.appendChild(card)}const html=`<div class="eyebrow">EXPLOSION ANALYTICS</div><h3 class="h3">エクスプロージョン確率</h3><div class="aaExHero"><div class="tiny" style="font-weight:900">現在の計算確率</div><div class="aaExOdds">${fmtPct(cur.totalPct)}</div><div class="aaExBreak">基礎 ${fmtPct(cur.basePct)} ／ 連続 ${signedPct(cur.streakPct)} ／ チャージ ${signedPct(cur.chargePct)} ／ 7日補正 ${signedPct(cur.qualityPct)} ／ 日替わり加算 ${signedPct(cur.driftPct)}</div><div class="aaExFormula"><small>計算式（基礎 + 連続 + チャージ + 7日補正 + 日替わり加算）</small>${formula(cur)}</div></div><div class="aaExToday"><div><b>${todayDraw?(todayDraw.explosion?'EXPLOSION':'通常'):'未抽選'}</b><span>今日の抽選結果</span></div><div><b>${todayDraw?fmtPct(todayDraw.oddsPct):'--'}</b><span>今日の抽選時確率</span></div></div>${todayDraw?`<div class="aaExFormula" style="margin-top:8px"><small>今日の抽選時の計算式</small>${formula(todayDraw)}</div>`:''}<div class="tiny" style="font-weight:900;margin-top:16px">EXPLOSION発生履歴</div><div style="margin-top:5px">${hits.length?hits.map(x=>`<div class="aaExHit"><div><span class="aaExBadge">EXPLOSION</span> <b>${fmtTime(x.at)}</b></div><div><b>${fmtPct(x.oddsPct)}</b><div class="tiny">発生時確率</div></div><div class="aaExHitFormula">発生時の計算式：${formula(x)}</div></div>`).join(''):'<div class="tiny" style="padding:10px 0">まだ発生履歴はありません。今後の抽選から、その時の確率と計算式を固定して記録します。</div>'}</div>`;const sig=JSON.stringify([today,cur.totalPct,todayDraw,h.draws.length,h.draws.at(-1)]);if(sig!==lastSig){lastSig=sig;card.innerHTML=html}}
function schedule(ms=0){clearTimeout(timer);timer=setTimeout(render,ms)}
document.addEventListener('companion7:daily-first-open',e=>{snapshotBeforeDraw(e.detail?.date||localDay());schedule(0)});
document.addEventListener('companion7:daily-draw',e=>{const d=e.detail||{},snap=pendingFor(d.date);saveDraw({...d,at:Date.now()},snap)});
document.addEventListener('companion7:study-streak',()=>schedule(0));
addEventListener('storage',e=>{if([STREAK_KEY,PICK_KEY,LOG_KEY].includes(e.key))schedule(0)});
function start(){css();const p=read(PICK_KEY,null);if(window.Companion7?.dailyPending&&p?.date!==localDay())snapshotBeforeDraw(localDay());recoverExistingPick();render();setInterval(()=>{recoverExistingPick();schedule(0)},2000)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();