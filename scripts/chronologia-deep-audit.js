'use strict';
const fs=require('fs'),vm=require('vm');
function read(name){return fs.readFileSync(name,'utf8')}
function extractJSONArray(source,marker){const p=source.indexOf(marker);if(p<0)throw new Error(marker+' not found');let i=source.indexOf('[',p),depth=0,str=false,esc=false;for(let j=i;j<source.length;j++){const c=source[j];if(str){if(esc)esc=false;else if(c==='\\')esc=true;else if(c==='"')str=false;continue}if(c==='"'){str=true;continue}if(c==='[')depth++;else if(c===']'){depth--;if(depth===0)return JSON.parse(source.slice(i,j+1))}}throw new Error('DATA array end not found')}
(async()=>{
 const html=read('chronologia.html'),base=extractJSONArray(html,'const DATA =');
 const ctx={console,window:{},document:{readyState:'loading',addEventListener(){},getElementById(){return null},querySelector(){return null},createElement(){return{style:{},appendChild(){}}}},setInterval(){return 0},clearInterval(){},CustomEvent:function(){},atob,Blob,DecompressionStream,Response,Uint8Array};ctx.window=ctx;vm.createContext(ctx);
 for(const f of ['chronologia-v7-data-1.js','chronologia-v7-data-2a.js','chronologia-v7-data-2b.js','chronologia-v7-data-3.js','chronologia-v7-data-4.js','chronologia-v7-overrides.js'])vm.runInContext(read(f),ctx,{filename:f});if(ctx.CHRONO_V7_EXTRA_READY)await ctx.CHRONO_V7_EXTRA_READY;
 const data=[...base],seen=new Set(base.map(x=>Number(x.id))),rich={};for(const pack of ctx.CHRONO_V7_PACKS||[]){for(const x of pack.items||[]){if(seen.has(Number(x.id)))continue;seen.add(Number(x.id));data.push(x)}Object.assign(rich,pack.notes||{})}
 ctx.DATA=data;ctx.RICH_NOTES=rich;vm.runInContext(read('chronologia-deep-explanations-v3.js'),ctx,{filename:'chronologia-deep-explanations-v3.js'});const map=vm.runInContext('ChronologiaDeepExplanations.build() || window.CHRONOLOGIA_DEEP_NOTES_BY_ID || ({})',ctx);
 const fields=['summary','background','result','exam','asahi','trap'],mins={summary:70,background:90,result:80,exam:100,asahi:90,trap:45},fail=[],stats={};for(const f of fields)stats[f]={min:1e9,max:0,total:0,under:0};let deepUnder=0,oldGeneric=0,supplementGeneric=0;
 for(const item of data){const n=map[Number(item.id)];if(!n){fail.push('missing:'+item.id);continue}for(const f of fields){const len=String(n[f]||'').length,s=stats[f];s.min=Math.min(s.min,len);s.max=Math.max(s.max,len);s.total+=len;if(len<mins[f]){s.under++;fail.push(`${f}-short:${item.id}:${len}`)}}if(!Array.isArray(n.deepFacts)||n.deepFacts.length<6){deepUnder++;fail.push('deepFacts:'+item.id)}const joined=fields.map(f=>n[f]||'').join(' ');if(joined.includes('政治・社会・外交の変化が積み重なっていた')){oldGeneric++;fail.push('old-generic:'+item.id)}if(Number(item.id)>=501&&String(item.detail||'').includes('Chronologia 7の補充項目として')){supplementGeneric++;fail.push('supplement-generic:'+item.id)}}
 for(const f of fields){stats[f].avg=+(stats[f].total/Math.max(1,data.length)).toFixed(1);delete stats[f].total;if(stats[f].min===1e9)stats[f].min=0}const samples=data.filter(x=>/大化の改新|承久の乱|応仁の乱|明治維新|日本国憲法|楔形文字|乙巳の変/.test(x.event)).slice(0,10).map(x=>({id:x.id,date:x.date,event:x.event,detail:x.detail,note:map[Number(x.id)]}));
 const result={version:'3.0.0',engine:ctx.ChronologiaDeepExplanations?.version,items:data.length,generated:Object.keys(map).length,curatedPackNotes:Object.keys(rich).length,fieldStats:stats,deepFactsUnder6:deepUnder,oldGenericCount:oldGeneric,supplementGenericDetails:supplementGeneric,failures:fail.slice(0,50),failureCount:fail.length,samples,pass:data.length>=1000&&Object.keys(map).length===data.length&&supplementGeneric===0&&fail.length===0};console.log('CHRONOLOGIA_DEEP_AUDIT '+JSON.stringify(result));if(!result.pass)process.exitCode=1;
})().catch(e=>{console.error('CHRONOLOGIA_DEEP_AUDIT_ERROR',e);process.exitCode=1});