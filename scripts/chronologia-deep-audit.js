'use strict';
const fs=require('fs'),vm=require('vm');
function read(name){return fs.readFileSync(name,'utf8')}
function extractJSONArray(source,marker){const p=source.indexOf(marker);if(p<0)throw new Error(marker+' not found');let i=source.indexOf('[',p),depth=0,str=false,esc=false;for(let j=i;j<source.length;j++){const c=source[j];if(str){if(esc)esc=false;else if(c==='\\')esc=true;else if(c==='"')str=false;continue}if(c==='"'){str=true;continue}if(c==='[')depth++;else if(c===']'){depth--;if(depth===0)return JSON.parse(source.slice(i,j+1))}}throw new Error('DATA array end not found')}
(async()=>{
 const html=read('chronologia.html'),base=extractJSONArray(html,'const DATA =');
 const ctx={console,window:{},document:{readyState:'loading',addEventListener(){},getElementById(){return null},querySelector(){return null},createElement(){return{style:{},appendChild(){}}}},setInterval(){return 0},clearInterval(){},setTimeout(fn){fn();return 0},CustomEvent:function(){},atob,Blob,DecompressionStream,Response,Uint8Array};ctx.window=ctx;vm.createContext(ctx);
 const packFiles=['chronologia-v7-data-1.js','chronologia-v7-data-2a.js','chronologia-v7-data-2b.js','chronologia-v7-data-3.js','chronologia-v7-data-4.js','chronologia-v7-overrides.js'];
 for(const f of packFiles)vm.runInContext(read(f),ctx,{filename:f});if(ctx.CHRONO_V7_EXTRA_READY)await ctx.CHRONO_V7_EXTRA_READY;
 const curatedFiles=['chronologia-curated-501-600.js','chronologia-curated-601-700.js','chronologia-curated-701-800.js','chronologia-curated-801-900.js','chronologia-curated-901-1000.js','chronologia-curated-final-fixes.js'];
 for(const f of curatedFiles)vm.runInContext(read(f),ctx,{filename:f});
 const data=[...base],seen=new Set(base.map(x=>Number(x.id))),rich={};for(const pack of ctx.CHRONO_V7_PACKS||[]){for(const x of pack.items||[]){if(seen.has(Number(x.id)))continue;seen.add(Number(x.id));data.push(x)}Object.assign(rich,pack.notes||{})}ctx.DATA=data;ctx.RICH_NOTES=rich;
 vm.runInContext(read('chronologia-deep-explanations-v3.js'),ctx,{filename:'chronologia-deep-explanations-v3.js'});vm.runInContext('ChronologiaDeepExplanations.build()',ctx);vm.runInContext(read('chronologia-deep-polish-v1.js'),ctx,{filename:'chronologia-deep-polish-v1.js'});vm.runInContext('ChronologiaDeepPolish.run()',ctx);const map=ctx.CHRONOLOGIA_DEEP_NOTES_BY_ID||{};
 const fields=['summary','background','result','exam','asahi','trap'],mins={summary:70,background:90,result:80,exam:100,asahi:90,trap:45},fail=[],stats={};for(const f of fields)stats[f]={min:1e9,max:0,total:0,under:0};let deepUnder=0,oldGeneric=0,supplementGeneric=0,curatedShort=0,curatedTagUnder3=0,curatedCount=0;
 const genericNeedles=['Chronologia 7の補充項目として','社会・時代変化の流れに位置する出来事で、時期・地域・前後の変化を組み合わせて意味を説明することが重要である'];
 for(const item of data){const n=map[Number(item.id)];if(!n){fail.push('missing:'+item.id);continue}for(const f of fields){const len=String(n[f]||'').length,s=stats[f];s.min=Math.min(s.min,len);s.max=Math.max(s.max,len);s.total+=len;if(len<mins[f]){s.under++;fail.push(`${f}-short:${item.id}:${len}`)}}if(!Array.isArray(n.deepFacts)||n.deepFacts.length<6){deepUnder++;fail.push('deepFacts:'+item.id)}const joined=fields.map(f=>n[f]||'').join(' ');if(joined.includes('政治・社会・外交の変化が積み重なっていた')){oldGeneric++;fail.push('old-generic:'+item.id)}if(Number(item.id)>=501){curatedCount++;const detail=String(item.detail||'');if(genericNeedles.some(x=>detail.includes(x))){supplementGeneric++;fail.push('supplement-generic:'+item.id)}if(detail.length<70){curatedShort++;fail.push(`curated-detail-short:${item.id}:${detail.length}`)}if(!Array.isArray(item.tags)||item.tags.length<3){curatedTagUnder3++;fail.push(`curated-tags:${item.id}:${item.tags?.length||0}`)}}}
 for(const f of fields){stats[f].avg=+(stats[f].total/Math.max(1,data.length)).toFixed(1);delete stats[f].total;if(stats[f].min===1e9)stats[f].min=0}
 const byId=new Map(data.map(x=>[Number(x.id),x]));for(const [id,key,want] of [[762,'date','1790年'],[763,'date','1797年'],[836,'date','1871年'],[979,'date','1969年']]){const got=String(byId.get(id)?.[key]||'');if(got!==want)fail.push(`correction:${id}:${key}:${got}->${want}`)}if(!String(byId.get(895)?.event||'').startsWith('鉱山法'))fail.push('correction:895:event');if(!String(byId.get(979)?.event||'').includes('策定'))fail.push('correction:979:event');
 const curatedStats=[ctx.CHRONOLOGIA_CURATED_501_600_STATS,ctx.CHRONOLOGIA_CURATED_601_700_STATS,ctx.CHRONOLOGIA_CURATED_701_800_STATS,ctx.CHRONOLOGIA_CURATED_801_900_STATS,ctx.CHRONOLOGIA_CURATED_901_1000_STATS];curatedStats.forEach((s,i)=>{if(!s?.pass||s.changed!==100)fail.push(`curated-pack-${i+1}:${JSON.stringify(s||null)}`)});const finalFix=ctx.CHRONOLOGIA_CURATED_FINAL_FIX_STATS;if(!finalFix?.pass||finalFix.changed!==1)fail.push(`curated-final-fix:${JSON.stringify(finalFix||null)}`);
 const samples=[501,537,550,562,615,642,684,732,762,823,863,895,940,966,979,997,1000].map(id=>{const x=byId.get(id);return{id,date:x?.date,event:x?.event,detail:x?.detail,tags:x?.tags,note:map[id]}});
 const result={version:'4.1.0',engine:ctx.ChronologiaDeepExplanations?.version,polish:ctx.CHRONOLOGIA_DEEP_POLISH_STATS,items:data.length,generated:Object.keys(map).length,curatedPackNotes:Object.keys(rich).length,handCuratedSupplementItems:curatedCount,curatedPacks:curatedStats,finalFix,curatedDetailUnder70:curatedShort,curatedTagsUnder3:curatedTagUnder3,fieldStats:stats,deepFactsUnder6:deepUnder,oldGenericCount:oldGeneric,supplementGenericDetails:supplementGeneric,failures:fail.slice(0,80),failureCount:fail.length,samples,pass:data.length===1000&&Object.keys(map).length===1000&&curatedCount===500&&curatedShort===0&&curatedTagUnder3===0&&supplementGeneric===0&&curatedStats.every(s=>s?.pass&&s.changed===100)&&finalFix?.pass&&fail.length===0};console.log('CHRONOLOGIA_DEEP_AUDIT '+JSON.stringify(result));if(!result.pass)process.exitCode=1;
})().catch(e=>{console.error('CHRONOLOGIA_DEEP_AUDIT_ERROR',e);process.exitCode=1});